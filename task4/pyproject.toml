[project]
name = "tasks4"
version = "0.1.0"
description = "Experiment using OpenAI Chat Completions API to summarize tasks"
requires-python = ">=3.10"
dependencies = ["openai"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
