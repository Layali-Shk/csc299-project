# Final Project - StudyBuddy AI (Final)
This is Layali's final project: a terminal-based PKMS + Task Manager + AI agents.

Run:
```bash
python -m final_app.main
```

Requirements:
- Python 3.9+
- Optional: set OPENAI_API_KEY to enable AI agents (uses OpenAI python package).
