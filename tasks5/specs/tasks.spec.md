
# Tasks System Specification

## Overview
System to create, list, complete, and delete tasks.

## Requirements
- A task has: id, title, completed flag
- Tasks are stored in memory
- Functions:
  - add_task(title)
  - list_tasks()
  - complete_task(id)
  - delete_task(id)
